# Verifier Fixtures

Canonical executable verifier fixtures live in `tests/data/` and are mirrored here for production-shape layout alignment.

Included examples:
- `verifier_resume_taskspec_example.json`
- `verifier_resume_pipeline_example.json`

Generated execution artifact:
- `tests/data/verifier_resume_execution_output.json`
